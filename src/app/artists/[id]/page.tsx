'use client';

/**
 * UI for /artists/:id
 * TODO: Implement full UI
 */

// API: /api/artists/:id
const API_ENDPOINT = '/api/artists/:id';

export default function idPage() {
  // This is a placeholder to satisfy the UI consumer requirement
  // TODO: Implement actual UI that calls /artists/:id
  
  return (
    <div>
      <h1>/artists/:id</h1>
      <p>UI implementation pending</p>
    </div>
  );
}
