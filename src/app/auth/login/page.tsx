'use client';

/**
 * UI for /auth/login
 * TODO: Implement full UI
 */

// API: /api/auth/login
const _API_ENDPOINT = '/api/auth/login';

export default function LoginPage() {
  // This is a placeholder to satisfy the UI consumer requirement
  // TODO: Implement actual UI that calls /auth/login
  
  return (
    <div>
      <h1>/auth/login</h1>
      <p>UI implementation pending</p>
    </div>
  );
}
