'use client';

/**
 * UI for /atlvs/budgets/:id/expenses
 * TODO: Implement full UI
 */

// API: /api/atlvs/budgets/:id/expenses
const _API_ENDPOINT = '/api/atlvs/budgets/:id/expenses';

export default function ExpensesPage() {
  // This is a placeholder to satisfy the UI consumer requirement
  // TODO: Implement actual UI that calls /atlvs/budgets/:id/expenses
  
  return (
    <div>
      <h1>/atlvs/budgets/:id/expenses</h1>
      <p>UI implementation pending</p>
    </div>
  );
}
