'use client';

/**
 * UI for /social/posts/:id/comments
 * TODO: Implement full UI
 */

// API: /api/social/posts/:id/comments
const API_ENDPOINT = '/api/social/posts/:id/comments';

export default function CommentsPage() {
  // This is a placeholder to satisfy the UI consumer requirement
  // TODO: Implement actual UI that calls /social/posts/:id/comments
  
  return (
    <div>
      <h1>/social/posts/:id/comments</h1>
      <p>UI implementation pending</p>
    </div>
  );
}
