'use client';

/**
 * UI for /events/:id/artists
 * TODO: Implement full UI
 */

// API: /api/events/:id/artists
const _API_ENDPOINT = '/api/events/:id/artists';

export default function ArtistsPage() {
  // This is a placeholder to satisfy the UI consumer requirement
  // TODO: Implement actual UI that calls /events/:id/artists
  
  return (
    <div>
      <h1>/events/:id/artists</h1>
      <p>UI implementation pending</p>
    </div>
  );
}
