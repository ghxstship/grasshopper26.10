import { prisma } from '@/lib/prisma';

/**
 * [id]Service
 * Service layer for /spotify/artists/:id
 */
export class SpotifyService {
  async execute(data: any) {
    // Implementation here
    return data;
  }
}
